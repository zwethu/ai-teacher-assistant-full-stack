Loading spinners. `Spinner` is the inline Loader2 ring; `PageSpinner` centers it with a label for full-panel states.

```jsx
<Spinner size={16} />
<Button loading>Generating…</Button>
<PageSpinner label="Checking sign-in status…" />
```
