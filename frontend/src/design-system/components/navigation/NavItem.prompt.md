Sidebar navigation item with lifted active/hover states (azure gradient). Render as an anchor (`href`) or button.

```jsx
<NavItem icon={<MessageCircle size={20} />} label="Chat" active href="/chat" />
<NavItem icon={<Users size={20} />} label="Batches" href="/batches" />
<NavItem icon={<Clock size={20} />} label="Sessions" badge={3} href="/chat-history" />
<NavItem icon={<Mail size={20} />} label="Send Emails" collapsed />
```
